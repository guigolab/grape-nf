// Process configuration
process {
    withName: markdup {
        ext {
            tool = 'sambamba'
            version = '0.6.8'
            profile = { params.removeDuplicates ? "-remove" : "" }
        }
    }
}